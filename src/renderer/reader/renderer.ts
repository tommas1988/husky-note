import { lexingListener, LexingEvent } from '../editor/markdown/markdown';

export default class Renderer {
    constructor() {

    }

    private handleHeaderToken(lineIndex: number, level: number, content: string) {

    }

    private handleQouteToken(lineIndex: number, depth: number, content: string) {

    }

    private handleListToken(lineIndex: number, content: string) {

    }

    private handleEnterCodeBlockToken(lineIndex: number, lang: string) {

    }

    private handleCodeBlockToken(lineIndex: number, codes: string) {

    }

    private handleExitCodeBlockToken(lineIndex: number) {

    }

    private handleCodeInlineToken(lineIndex: number, code: string) {

    }

    private handleEmphasisToken(lineIndex: number, content: string) {

    }

    private handleStrongToken(lineIndex: number, content: string) {

    }

    private handleLinkToken(lineIndex: number, ) {

    }

    private handleImageToken(lineIndex: number) {

    }

    private handleHorizontalToken(lineIndex: number) {

    }

    private handleEscapeToken(lineIndex: number) {

    }

    private handleHtmlBeginTagToken(lineIndex: number) {

    }

    private handleHtmlEndTagToken(lineIndex: number) {

    }

    private hanleHtmlSelfCloseToken(lineIndex: number) {

    }

    private handleParagraphToken(lineIndex: number) {

    }
}